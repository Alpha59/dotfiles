---
syntax: bash
tags: [bash, personal]
version: 1.0.0
default: true
---


    EXAMPLES:
# Prints version information
pssh --version;
# Runs script in verbose mode
pssh -v;
# Runs script in quiet mode
pssh -q;
# pipes output into a file
pssh -o file.txt;

# Run the command against all hosts in the host file
pssh host-file "command"

# Grep the logs on all hosts
pssh host-file -g "search-term"

