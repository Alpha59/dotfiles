---
syntax: bash
tags: [bash, personal]
version: 1.0.0
default: true
---

        
    EXAMPLES:
# Prints version information
redrive-dlq --version;
# Runs script in verbose mode
redrive-dlq -v;
# Runs script in quiet mode
redrive-dlq -q;
# pipes output into a file
redrive-dlq -o file.txt;

# Redrive DQL
redrive-dql $ACCOUNT $DLQ $QUEUE 

# Force Purge
redrive-dql $ACCOUNT $DLQ $QUEUE -p

