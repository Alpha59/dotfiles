---
syntax: bash
tags: [bash, personal]
version: 1.0.0
default: NO
---

    EXAMPLES:
# Print batter status 
battery-status;

# Prints version information
battery-status --version;
# Runs script in verbose mode
battery-status -v;
# Runs script in quiet mode
battery-status -q;
# pipes output into a file
battery-status -o file.txt;

